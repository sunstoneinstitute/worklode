{
  "name": "lode",
  "version": "0.10.0",
  "description": "Worklode: worktree-bound task pickup for agents. /lode:add files a new task from a short problem description, after settling its kind and confirming a summary. Slash commands /lode:next|resume|done|block|status wrap the lode CLI to claim a ready task, create its worktree under .worktrees/, bind the lease, and hand it off for review or record it blocked. /lode:spec-coverage finds accepted specs with sections no accepted plan covers and seeds a planning task for each; /lode:plan-spec decomposes an accepted spec into plans under a claimed design task. The `worklode` skill is a model-invocable orientation reference for any project Worklode manages — entities, edges, the task state machine, the doc model, the full command catalog — meant to be pointed at from a project's own CLAUDE.md. Lifecycle hooks ship with the CLI, not here: `lode install` writes them per repo into each targeted coding agent's own configuration, alongside the git hooks and the AGENTS.md block, so they stay versioned with the binary.",
  "author": {
    "name": "Stig Bakken",
    "email": "stig@sunstone.institute"
  },
  "keywords": [
    "worklode",
    "lode",
    "worktree",
    "task-pickup",
    "lease",
    "agent-workflow",
    "cli",
    "onboarding"
  ],
  "skills": "./skills/",
  "interface": {
    "displayName": "Lode",
    "shortDescription": "Worklode: worktree-bound task pickup for agents",
    "longDescription": "Worklode: worktree-bound task pickup for agents. /lode:add files a new task from a short problem description, after settling its kind and confirming a summary. Slash commands /lode:next|resume|done|block|status wrap the lode CLI to claim a ready task, create its worktree under .worktrees/, bind the lease, and hand it off for review or record it blocked. /lode:spec-coverage finds accepted specs with sections no accepted plan covers and seeds a planning task for each; /lode:plan-spec decomposes an accepted spec into plans under a claimed design task. The `worklode` skill is a model-invocable orientation reference for any project Worklode manages — entities, edges, the task state machine, the doc model, the full command catalog — meant to be pointed at from a project's own CLAUDE.md. Lifecycle hooks ship with the CLI, not here: `lode install` writes them per repo into each targeted coding agent's own configuration, alongside the git hooks and the AGENTS.md block, so they stay versioned with the binary.",
    "developerName": "Stig Bakken",
    "category": "Productivity",
    "capabilities": [
      "Instructions"
    ],
    "defaultPrompt": [
      "Help me use the Lode plugin."
    ]
  }
}
