import { Plugin } from "obsidian";

// Stub entry point. Task 9 replaces this with the real plugin: settings,
// commands and the sync trigger wired to src/sync/mirror.ts.
export default class WorklodePlugin extends Plugin {
  override async onload() {}
}
