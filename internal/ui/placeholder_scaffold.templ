package ui

// scaffoldPing is a throwaway component with no purpose beyond giving
// `templ generate` and the tailwind @source glob a .templ file to match
// while internal/ui carries no real page components yet (Task 1 of
// .superpowers/sdd/plan-internal-ui-cockpit/). Delete it once Task 2 lands
// real components, unless something still references it by then.
templ scaffoldPing() {
	<span></span>
}
